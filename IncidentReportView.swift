import SwiftUI

struct IncidentReportView: View {
    var body: some View {
        VStack {
            Text("Sentinel")
                .font(.title)
            Text("AI-Powered Safety Reporting")
                .foregroundColor(.gray)
        }
        .padding()
    }
}
